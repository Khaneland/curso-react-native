import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    ex: {
        paddingHorizontal: 5,
        marginVertical: 15,
        borderRadius: 10,
        borderWidth: 2,
        borderColor: '#222',
        fontSize: 24,
        fontWeight: 'bold'
    },
    input: {
        height: 70,
        fontSize: 40,
        borderColor: 'gray',
        borderWidth: 1
    },
    fonte40: {
        fontSize: 40,
    }
});

export default styles;
